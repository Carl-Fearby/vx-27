import "../globals.css";

export const metadata = {
  title: "Play — VX-27",
  description: "First-person arena combat in the browser.",
};

export default function GameLayout({ children }) {
  return children;
}
